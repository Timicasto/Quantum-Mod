package timicasto.quantumbase.fluid;

/* public class FluidLoader {

     public static Fluid petroleum = new LiquidPetroleum();

    public FluidLoader(FMLPreInitializationEvent event)
    {
        if (FluidRegistry.isFluidRegistered(petroleum))
        {
            petroleum = FluidRegistry.getFluid(petroleum.getName());
        }
        else
        {
            FluidRegistry.registerFluid(petroleum);
        }
    }
} */
